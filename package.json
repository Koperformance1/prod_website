{
  "name": "mern-project",
  "version": "1.0.0",
  "main": "server/server.js",
  "scripts": {
    "dev": "concurrently \"cd server && npm run dev\" \"cd client && npm start\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "setup": "cd server && npm install && cd ../client && npm install",
    "build": "cd client && npm install && CI=false npm run build && cd ../server && npm install && mkdir -p client && cp -r ../client/build client/",
    "start": "cd server && npm start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "axios": "^1.11.0",
    "concurrently": "^8.2.2"
  },
  "devDependencies": {
    "@babel/plugin-proposal-private-property-in-object": "^7.21.11",
    "nodemon": "^3.1.9"
  },
  "engines": {
    "node": "18.x",
    "npm": "10.x"
  }
}
